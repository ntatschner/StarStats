# StarStats — design handoff (read-only mirror)

This folder is a **verbatim copy** of the design handoff package shipped on
2026-05-05 (`StarStats (1).zip`). Treat it as read-only reference material —
the working copies of any adopted assets live under `apps/web/src/styles/`
and `apps/web/src/components/`.

The original `README.md` and `DESIGN_REFERENCE.md` here are the design
authors' instructions. Do not edit. If a design decision changes, update
the working copies in `apps/web/` and note the divergence in
`docs/DESIGN-REDESIGN-PLAN.md`.

For the implementation plan, see [`docs/DESIGN-REDESIGN-PLAN.md`](../docs/DESIGN-REDESIGN-PLAN.md).
